# Supabase (public anon key) - do NOT commit real values
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: used for redirect after sign up in dev
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=

# Next.js runtime environment (optional)
NODE_ENV=development

# Add other provider keys here (example placeholders)
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_MAPBOX_TOKEN=
